{{ 'section-image-banner.css' | asset_url | stylesheet_tag }}
{{ 'component-card.css' | asset_url | stylesheet_tag }}
{{ 'component-price.css' | asset_url | stylesheet_tag }}

<style>
  /* Reset and Base Styles */
  .homepage-container {
    margin: 0;
    padding: 0;
  }

  /* Hero Section - Exact boat-lifestyle.com Match */
  .homepage-hero {
    background: linear-gradient(
        135deg,
        rgba(26, 26, 46, 0.85) 0%,
        rgba(22, 33, 62, 0.9) 50%,
        rgba(26, 26, 46, 0.85) 100%
      ),
      url('https://images.unsplash.com/photo-1588681664899-f142ff2dc9b1?q=80&w=1200&auto=format&fit=crop') center/cover;
    color: white;
    padding: 140px 0 120px 0;
    text-align: left;
    position: relative;
    overflow: hidden;
    min-height: 600px;
    display: flex;
    align-items: center;
  }

  .homepage-hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(ellipse at 60% 40%, rgba(217, 35, 45, 0.15) 0%, transparent 60%),
      radial-gradient(ellipse at 20% 80%, rgba(0, 181, 148, 0.1) 0%, transparent 50%);
    z-index: 1;
  }

  .homepage-hero .page-width {
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
    min-height: 400px;
  }

  .hero-content {
    max-width: 600px;
    padding-left: 40px;
  }

  .hero-badge {
    display: inline-block;
    background: rgba(217, 35, 45, 0.9);
    color: white;
    padding: 8px 20px;
    border-radius: 25px;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 20px;
    backdrop-filter: blur(10px);
  }

  .hero-title {
    font-size: clamp(52px, 8vw, 96px);
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: -2px;
    margin-bottom: 8px;
    background: linear-gradient(135deg, #ffffff 0%, #f0f0f0 100%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    text-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
    line-height: 0.9;
  }

  .hero-subtitle {
    font-size: clamp(28px, 5vw, 56px);
    font-weight: 900;
    margin-bottom: 12px;
    color: #ff4444;
    text-transform: uppercase;
    letter-spacing: -1px;
    line-height: 0.9;
    text-shadow: 0 2px 10px rgba(255, 68, 68, 0.3);
  }

  .hero-description {
    font-size: clamp(16px, 2vw, 20px);
    font-weight: 500;
    margin-bottom: 40px;
    opacity: 0.95;
    letter-spacing: 0.5px;
  }

  .hero-buttons {
    display: flex;
    gap: 20px;
    flex-wrap: wrap;
  }

  .hero-button {
    background: linear-gradient(135deg, #d9232d 0%, #b01d26 100%);
    color: white;
    padding: 18px 36px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    font-size: 16px;
    transition: all 0.3s ease;
    display: inline-block;
    border: none;
    box-shadow: 0 4px 20px rgba(217, 35, 45, 0.3);
  }

  .hero-button:hover {
    background: linear-gradient(135deg, #ff4444 0%, #d9232d 100%);
    transform: translateY(-3px);
    box-shadow: 0 12px 35px rgba(217, 35, 45, 0.4);
  }

  .hero-button--secondary {
    background: transparent;
    border: 2px solid rgba(255, 255, 255, 0.8);
    color: white;
    box-shadow: 0 4px 20px rgba(255, 255, 255, 0.1);
  }

  .hero-button--secondary:hover {
    background: rgba(255, 255, 255, 0.95);
    color: #1a1a2e;
    border-color: white;
    transform: translateY(-3px);
    box-shadow: 0 12px 35px rgba(255, 255, 255, 0.2);
  }

  /* Product Carousel Sections */
  .product-carousel-section {
    padding: 80px 0;
    background: #0a0a0a;
    position: relative;
  }

  .product-carousel-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 25%, #2d2d2d 50%, #1a1a1a 75%, #0a0a0a 100%);
    z-index: 1;
  }

  .product-carousel-section .page-width {
    position: relative;
    z-index: 2;
  }

  .carousel-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 40px;
  }

  .carousel-title {
    font-size: clamp(28px, 4vw, 42px);
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: white;
    margin: 0;
  }

  .view-all-link {
    color: #d9232d;
    text-decoration: none;
    font-weight: 600;
    font-size: 16px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    transition: color 0.3s ease;
  }

  .view-all-link:hover {
    color: #ff4444;
  }

  .product-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 24px;
  }

  .product-card {
    background: #1a1a1a;
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.08);
    transition: all 0.3s ease;
    position: relative;
  }

  .product-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.4);
    border-color: rgba(217, 35, 45, 0.3);
  }

  .product-image {
    aspect-ratio: 1;
    overflow: hidden;
    position: relative;
  }

  .product-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }

  .product-card:hover .product-image img {
    transform: scale(1.05);
  }

  .discount-badge {
    position: absolute;
    top: 12px;
    right: 12px;
    background: #d9232d;
    color: white;
    padding: 6px 12px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .product-info {
    padding: 20px;
  }

  .product-title {
    margin: 0 0 12px 0;
    font-size: 16px;
    font-weight: 600;
    color: white;
    line-height: 1.3;
  }

  .product-price {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 16px;
  }

  .current-price {
    font-size: 20px;
    font-weight: 700;
    color: #d9232d;
  }

  .compare-price {
    font-size: 14px;
    color: #999;
    text-decoration: line-through;
  }

  .add-to-cart-btn {
    width: 100%;
    background: #d9232d;
    color: white;
    border: none;
    padding: 12px;
    border-radius: 8px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .add-to-cart-btn:hover {
    background: #b01d26;
    transform: translateY(-2px);
  }

  /* Promotional Banner Sections */
  .promo-banner {
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #1a1a2e 100%);
    padding: 80px 0;
    text-align: center;
    color: white;
    position: relative;
    overflow: hidden;
  }

  .promo-banner::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, rgba(217, 35, 45, 0.1) 0%, transparent 50%, rgba(217, 35, 45, 0.1) 100%);
    z-index: 1;
  }

  .promo-banner .page-width {
    position: relative;
    z-index: 2;
  }

  .promo-badge {
    display: inline-block;
    background: #d9232d;
    color: white;
    padding: 12px 24px;
    border-radius: 25px;
    font-size: 14px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 20px;
  }

  .promo-title {
    font-size: clamp(42px, 7vw, 84px);
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: -2px;
    margin-bottom: 16px;
    background: linear-gradient(135deg, #ffffff 0%, #f0f0f0 100%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .promo-subtitle {
    font-size: clamp(18px, 3vw, 28px);
    font-weight: 600;
    margin-bottom: 32px;
    opacity: 0.9;
  }

  /* Newsletter Section */
  .newsletter-section {
    background: #f8f8f8;
    padding: 80px 0;
    text-align: center;
  }

  .newsletter-title {
    font-size: clamp(28px, 4vw, 42px);
    font-weight: 900;
    color: #1a1a1a;
    margin-bottom: 16px;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .newsletter-description {
    font-size: 18px;
    color: #666;
    margin-bottom: 40px;
  }

  .newsletter-form {
    display: flex;
    max-width: 500px;
    margin: 0 auto;
    gap: 16px;
  }

  .newsletter-input {
    flex: 1;
    padding: 16px 20px;
    border: 2px solid #ddd;
    border-radius: 8px;
    font-size: 16px;
    background: white;
  }

  .newsletter-input:focus {
    outline: none;
    border-color: #d9232d;
  }

  .newsletter-button {
    background: #d9232d;
    color: white;
    border: none;
    padding: 16px 32px;
    border-radius: 8px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .newsletter-button:hover {
    background: #b01d26;
    transform: translateY(-2px);
  }

  /* Responsive Design */
  @media screen and (max-width: 989px) {
    .carousel-header {
      flex-direction: column;
      gap: 20px;
      text-align: center;
    }

    .product-grid {
      grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
      gap: 20px;
    }

    .homepage-hero {
      text-align: center;
      padding: 100px 0 80px 0;
    }

    .hero-content {
      padding-left: 0;
      max-width: 100%;
    }

    .hero-title {
      font-size: clamp(42px, 7vw, 72px);
    }

    .hero-subtitle {
      font-size: clamp(24px, 4vw, 42px);
    }
  }

  @media screen and (max-width: 749px) {
    .homepage-hero {
      padding: 80px 0;
      min-height: 500px;
    }

    .hero-content {
      padding: 20px;
    }

    .hero-title {
      font-size: clamp(36px, 8vw, 64px);
      margin-bottom: 12px;
    }

    .hero-subtitle {
      font-size: clamp(20px, 5vw, 36px);
      margin-bottom: 16px;
    }

    .hero-description {
      font-size: clamp(14px, 3vw, 18px);
      margin-bottom: 30px;
    }

    .hero-buttons {
      flex-direction: column;
      align-items: center;
      gap: 16px;
    }

    .hero-button {
      width: 100%;
      max-width: 280px;
      padding: 16px 24px;
      font-size: 15px;
    }

    .product-carousel-section {
      padding: 60px 0;
    }

    .carousel-title {
      font-size: clamp(24px, 5vw, 32px);
      margin-bottom: 20px;
    }

    .promo-banner {
      padding: 60px 0;
    }

    .promo-title {
      font-size: clamp(32px, 8vw, 56px);
    }

    .promo-subtitle {
      font-size: clamp(16px, 4vw, 20px);
    }

    .newsletter-section {
      padding: 60px 0;
    }

    .newsletter-form {
      flex-direction: column;
    }

    .newsletter-input,
    .newsletter-button {
      width: 100%;
    }

    .product-grid {
      grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
      gap: 16px;
    }

    .product-card {
      border-radius: 8px;
    }

    .product-info {
      padding: 16px;
    }

    .product-title {
      font-size: 15px;
    }

    .current-price {
      font-size: 18px;
    }

    .add-to-cart-btn {
      padding: 10px;
      font-size: 14px;
    }
  }

  @media screen and (max-width: 550px) {
    .homepage-hero {
      padding: 60px 0;
      min-height: 400px;
    }

    .hero-content {
      padding: 16px;
    }

    .hero-badge {
      font-size: 11px;
      padding: 6px 16px;
    }

    .hero-title {
      font-size: clamp(28px, 9vw, 48px);
      letter-spacing: -1px;
    }

    .hero-subtitle {
      font-size: clamp(18px, 6vw, 28px);
      letter-spacing: -0.5px;
    }

    .hero-description {
      font-size: clamp(13px, 3.5vw, 16px);
      margin-bottom: 24px;
    }

    .hero-button {
      max-width: 240px;
      padding: 14px 20px;
      font-size: 14px;
      letter-spacing: 0.5px;
    }

    .product-carousel-section {
      padding: 50px 0;
    }

    .carousel-header {
      margin-bottom: 30px;
    }

    .carousel-title {
      font-size: clamp(20px, 6vw, 28px);
    }

    .view-all-link {
      font-size: 14px;
    }

    .promo-banner {
      padding: 50px 0;
    }

    .promo-badge {
      font-size: 12px;
      padding: 8px 16px;
    }

    .promo-title {
      font-size: clamp(28px, 9vw, 42px);
    }

    .promo-subtitle {
      font-size: clamp(14px, 4vw, 18px);
      margin-bottom: 24px;
    }

    .newsletter-section {
      padding: 50px 0;
    }

    .newsletter-title {
      font-size: clamp(22px, 5vw, 32px);
    }

    .newsletter-description {
      font-size: 16px;
      margin-bottom: 30px;
    }

    .product-grid {
      grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
      gap: 12px;
    }
  }

  /* Performance Optimizations */
  .product-card,
  .hero-button,
  .add-to-cart-btn,
  .newsletter-button {
    will-change: transform;
  }

  .product-image img {
    will-change: transform;
  }

  /* Smooth scrolling */
  html {
    scroll-behavior: smooth;
  }

  /* Touch device optimizations */
  @media (hover: none) and (pointer: coarse) {
    .product-card:hover {
      transform: none;
    }

    .hero-button:hover,
    .add-to-cart-btn:hover,
    .newsletter-button:hover {
      transform: none;
    }

    .product-card:hover .product-image img {
      transform: none;
    }

    /* Ensure minimum touch target size */
    .hero-button,
    .add-to-cart-btn,
    .newsletter-button,
    .view-all-link {
      min-height: 44px;
      min-width: 44px;
    }
  }

  /* Reduce motion for users who prefer it */
  @media (prefers-reduced-motion: reduce) {
    .product-card,
    .hero-button,
    .add-to-cart-btn,
    .newsletter-button,
    .product-image img {
      transition: none;
    }

    .homepage-hero::before,
    .promo-banner::before,
    .product-carousel-section::before {
      animation: none;
    }
  }

  /* High contrast mode support */
  @media (prefers-contrast: high) {
    .hero-button,
    .add-to-cart-btn,
    .newsletter-button {
      border: 2px solid currentColor;
    }

    .product-card {
      border-width: 2px;
    }
  }

  /* Dark mode support */
  @media (prefers-color-scheme: dark) {
    .newsletter-section {
      background: #1a1a1a;
    }

    .newsletter-title {
      color: white;
    }

    .newsletter-description {
      color: #ccc;
    }

    .newsletter-input {
      background: #2d2d2d;
      border-color: #555;
      color: white;
    }
  }
</style>

<div class="homepage-container">
  <!-- Main Hero Section - Exact boat-lifestyle.com Design -->
  <section class="homepage-hero">
    <div class="page-width">
      <div class="hero-content">
        <div class="hero-badge">boAthead DAYS</div>
        <h1 class="hero-title">
          boAthead<br>
          DAYS
        </h1>
        <p class="hero-subtitle">get up to 75% OFF</p>
        <p class="hero-description">Extra 5% off | Use Code: BOATHEAD</p>
        <div class="hero-buttons">
          <a href="{{ routes.collections_url }}" class="hero-button">Shop Now</a>
          <a href="{{ routes.collections_url }}" class="hero-button hero-button--secondary">View Deals</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Big Deals Section -->
  <section class="product-carousel-section">
    <div class="page-width">
      <div class="carousel-header">
        <h2 class="carousel-title">Big Deals</h2>
        <a href="{{ routes.collections_url }}" class="view-all-link">View All →</a>
      </div>

      {% assign big_deals_collection = collections.all %}
      {% if collections['big-deals'] != blank %}
        {% assign big_deals_collection = collections['big-deals'] %}
      {% elsif collections['featured-products'] != blank %}
        {% assign big_deals_collection = collections['featured-products'] %}
      {% elsif collections.frontpage != blank %}
        {% assign big_deals_collection = collections.frontpage %}
      {% endif %}

      <div class="product-grid">
        {% for product in big_deals_collection.products limit: 6 %}
          <div class="product-card">
            {% if product.featured_media %}
              <div class="product-image">
                {{
                  product.featured_media
                  | image_url: width: 400
                  | image_tag: loading: 'lazy', width: '400', height: '400'
                }}
                {% if product.compare_at_price > product.price %}
                  {% assign discount = product.compare_at_price
                    | minus: product.price
                    | times: 100.0
                    | divided_by: product.compare_at_price
                    | round
                  %}
                  <div class="discount-badge">{{ discount }}% OFF</div>
                {% endif %}
              </div>
            {% else %}
              <div class="product-image">
                {{ 'product-1' | placeholder_svg_tag: 'placeholder-svg' }}
              </div>
            {% endif %}

            <div class="product-info">
              <h3 class="product-title">
                <a href="{{ product.url }}" style="color: inherit; text-decoration: none;">{{ product.title }}</a>
              </h3>
              <div class="product-price">
                <span class="current-price">{{ product.price | money }}</span>
                {% if product.compare_at_price > product.price %}
                  <span class="compare-price">{{ product.compare_at_price | money }}</span>
                {% endif %}
              </div>
              <button class="add-to-cart-btn" onclick="location.href='{{ product.url }}'">Add to Cart</button>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </section>

  <!-- Promotional Banner 1 -->
  <section class="promo-banner">
    <div class="page-width">
      <div class="promo-badge">boAthead DAYS</div>
      <h2 class="promo-title">75% OFF</h2>
      <p class="promo-subtitle">Extra 5% off | Use Code: BOATHEAD</p>
      <a href="{{ routes.collections_url }}" class="hero-button">Shop Collection</a>
    </div>
  </section>

  <!-- Top TWS Offers Section -->
  <section class="product-carousel-section">
    <div class="page-width">
      <div class="carousel-header">
        <h2 class="carousel-title">Top TWS Offers</h2>
        <a href="{{ routes.collections_url }}" class="view-all-link">View All →</a>
      </div>

      {% assign tws_collection = collections.all %}
      {% if collections['tws-offers'] != blank %}
        {% assign tws_collection = collections['tws-offers'] %}
      {% elsif collections['wireless-earbuds'] != blank %}
        {% assign tws_collection = collections['wireless-earbuds'] %}
      {% elsif collections.frontpage != blank %}
        {% assign tws_collection = collections.frontpage %}
      {% endif %}

      <div class="product-grid">
        {% for product in tws_collection.products limit: 6 %}
          <div class="product-card">
            {% if product.featured_media %}
              <div class="product-image">
                {{
                  product.featured_media
                  | image_url: width: 400
                  | image_tag: loading: 'lazy', width: '400', height: '400'
                }}
                {% if product.compare_at_price > product.price %}
                  {% assign discount = product.compare_at_price
                    | minus: product.price
                    | times: 100.0
                    | divided_by: product.compare_at_price
                    | round
                  %}
                  <div class="discount-badge">{{ discount }}% OFF</div>
                {% endif %}
              </div>
            {% else %}
              <div class="product-image">
                {{ 'product-2' | placeholder_svg_tag: 'placeholder-svg' }}
              </div>
            {% endif %}

            <div class="product-info">
              <h3 class="product-title">
                <a href="{{ product.url }}" style="color: inherit; text-decoration: none;">{{ product.title }}</a>
              </h3>
              <div class="product-price">
                <span class="current-price">{{ product.price | money }}</span>
                {% if product.compare_at_price > product.price %}
                  <span class="compare-price">{{ product.compare_at_price | money }}</span>
                {% endif %}
              </div>
              <button class="add-to-cart-btn" onclick="location.href='{{ product.url }}'">Add to Cart</button>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </section>

  <!-- Choose Your Offer Section -->
  <section class="product-carousel-section">
    <div class="page-width">
      <div class="carousel-header">
        <h2 class="carousel-title">Choose Your Offer</h2>
        <a href="{{ routes.collections_url }}" class="view-all-link">View All →</a>
      </div>

      {% assign offers_collection = collections.all %}
      {% if collections['special-offers'] != blank %}
        {% assign offers_collection = collections['special-offers'] %}
      {% elsif collections.sale != blank %}
        {% assign offers_collection = collections.sale %}
      {% elsif collections.frontpage != blank %}
        {% assign offers_collection = collections.frontpage %}
      {% endif %}

      <div class="product-grid">
        {% for product in offers_collection.products limit: 6 %}
          <div class="product-card">
            {% if product.featured_media %}
              <div class="product-image">
                {{
                  product.featured_media
                  | image_url: width: 400
                  | image_tag: loading: 'lazy', width: '400', height: '400'
                }}
                {% if product.compare_at_price > product.price %}
                  {% assign discount = product.compare_at_price
                    | minus: product.price
                    | times: 100.0
                    | divided_by: product.compare_at_price
                    | round
                  %}
                  <div class="discount-badge">{{ discount }}% OFF</div>
                {% endif %}
              </div>
            {% else %}
              <div class="product-image">
                {{ 'product-3' | placeholder_svg_tag: 'placeholder-svg' }}
              </div>
            {% endif %}

            <div class="product-info">
              <h3 class="product-title">
                <a href="{{ product.url }}" style="color: inherit; text-decoration: none;">{{ product.title }}</a>
              </h3>
              <div class="product-price">
                <span class="current-price">{{ product.price | money }}</span>
                {% if product.compare_at_price > product.price %}
                  <span class="compare-price">{{ product.compare_at_price | money }}</span>
                {% endif %}
              </div>
              <button class="add-to-cart-btn" onclick="location.href='{{ product.url }}'">Add to Cart</button>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </section>

  <!-- Promotional Banner 2 -->
  <section class="promo-banner">
    <div class="page-width">
      <div class="promo-badge">boAthead DAYS</div>
      <h2 class="promo-title">75% OFF</h2>
      <p class="promo-subtitle">Extra 5% off | Use Code: BOATHEAD</p>
      <a href="{{ routes.collections_url }}" class="hero-button">Shop Now</a>
    </div>
  </section>

  <!-- Unbelievable at best Prices Section -->
  <section class="product-carousel-section">
    <div class="page-width">
      <div class="carousel-header">
        <h2 class="carousel-title">Unbelievable at best Prices</h2>
        <a href="{{ routes.collections_url }}" class="view-all-link">View All →</a>
      </div>

      {% assign best_prices_collection = collections.all %}
      {% if collections['best-prices'] != blank %}
        {% assign best_prices_collection = collections['best-prices'] %}
      {% elsif collections.sale != blank %}
        {% assign best_prices_collection = collections.sale %}
      {% elsif collections.frontpage != blank %}
        {% assign best_prices_collection = collections.frontpage %}
      {% endif %}

      <div class="product-grid">
        {% for product in best_prices_collection.products limit: 6 %}
          <div class="product-card">
            {% if product.featured_media %}
              <div class="product-image">
                {{
                  product.featured_media
                  | image_url: width: 400
                  | image_tag: loading: 'lazy', width: '400', height: '400'
                }}
                {% if product.compare_at_price > product.price %}
                  {% assign discount = product.compare_at_price
                    | minus: product.price
                    | times: 100.0
                    | divided_by: product.compare_at_price
                    | round
                  %}
                  <div class="discount-badge">{{ discount }}% OFF</div>
                {% endif %}
              </div>
            {% else %}
              <div class="product-image">
                {{ 'product-4' | placeholder_svg_tag: 'placeholder-svg' }}
              </div>
            {% endif %}

            <div class="product-info">
              <h3 class="product-title">
                <a href="{{ product.url }}" style="color: inherit; text-decoration: none;">{{ product.title }}</a>
              </h3>
              <div class="product-price">
                <span class="current-price">{{ product.price | money }}</span>
                {% if product.compare_at_price > product.price %}
                  <span class="compare-price">{{ product.compare_at_price | money }}</span>
                {% endif %}
              </div>
              <button class="add-to-cart-btn" onclick="location.href='{{ product.url }}'">Add to Cart</button>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </section>

  <!-- Promotional Banner 3 -->
  <section class="promo-banner">
    <div class="page-width">
      <div class="promo-badge">boAthead DAYS</div>
      <h2 class="promo-title">75% OFF</h2>
      <p class="promo-subtitle">Extra 5% off | Use Code: BOATHEAD</p>
      <a href="{{ routes.collections_url }}" class="hero-button">Shop Collection</a>
    </div>
  </section>

  <!-- Exclusive Home Audio Steals Section -->
  <section class="product-carousel-section">
    <div class="page-width">
      <div class="carousel-header">
        <h2 class="carousel-title">Exclusive Home Audio Steals</h2>
        <a href="{{ routes.collections_url }}" class="view-all-link">View All →</a>
      </div>

      {% assign audio_collection = collections.all %}
      {% if collections['home-audio'] != blank %}
        {% assign audio_collection = collections['home-audio'] %}
      {% elsif collections.audio != blank %}
        {% assign audio_collection = collections.audio %}
      {% elsif collections.frontpage != blank %}
        {% assign audio_collection = collections.frontpage %}
      {% endif %}

      <div class="product-grid">
        {% for product in audio_collection.products limit: 6 %}
          <div class="product-card">
            {% if product.featured_media %}
              <div class="product-image">
                {{
                  product.featured_media
                  | image_url: width: 400
                  | image_tag: loading: 'lazy', width: '400', height: '400'
                }}
                {% if product.compare_at_price > product.price %}
                  {% assign discount = product.compare_at_price
                    | minus: product.price
                    | times: 100.0
                    | divided_by: product.compare_at_price
                    | round
                  %}
                  <div class="discount-badge">{{ discount }}% OFF</div>
                {% endif %}
              </div>
            {% else %}
              <div class="product-image">
                {{ 'product-5' | placeholder_svg_tag: 'placeholder-svg' }}
              </div>
            {% endif %}

            <div class="product-info">
              <h3 class="product-title">
                <a href="{{ product.url }}" style="color: inherit; text-decoration: none;">{{ product.title }}</a>
              </h3>
              <div class="product-price">
                <span class="current-price">{{ product.price | money }}</span>
                {% if product.compare_at_price > product.price %}
                  <span class="compare-price">{{ product.compare_at_price | money }}</span>
                {% endif %}
              </div>
              <button class="add-to-cart-btn" onclick="location.href='{{ product.url }}'">Add to Cart</button>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </section>

  <!-- Best Deals on Accessories Section -->
  <section class="product-carousel-section">
    <div class="page-width">
      <div class="carousel-header">
        <h2 class="carousel-title">Best Deals on Accessories</h2>
        <a href="{{ routes.collections_url }}" class="view-all-link">View All →</a>
      </div>

      {% assign accessories_collection = collections.all %}
      {% if collections.accessories != blank %}
        {% assign accessories_collection = collections.accessories %}
      {% elsif collections.deals != blank %}
        {% assign accessories_collection = collections.deals %}
      {% elsif collections.frontpage != blank %}
        {% assign accessories_collection = collections.frontpage %}
      {% endif %}

      <div class="product-grid">
        {% for product in accessories_collection.products limit: 6 %}
          <div class="product-card">
            {% if product.featured_media %}
              <div class="product-image">
                {{
                  product.featured_media
                  | image_url: width: 400
                  | image_tag: loading: 'lazy', width: '400', height: '400'
                }}
                {% if product.compare_at_price > product.price %}
                  {% assign discount = product.compare_at_price
                    | minus: product.price
                    | times: 100.0
                    | divided_by: product.compare_at_price
                    | round
                  %}
                  <div class="discount-badge">{{ discount }}% OFF</div>
                {% endif %}
              </div>
            {% else %}
              <div class="product-image">
                {{ 'product-6' | placeholder_svg_tag: 'placeholder-svg' }}
              </div>
            {% endif %}

            <div class="product-info">
              <h3 class="product-title">
                <a href="{{ product.url }}" style="color: inherit; text-decoration: none;">{{ product.title }}</a>
              </h3>
              <div class="product-price">
                <span class="current-price">{{ product.price | money }}</span>
                {% if product.compare_at_price > product.price %}
                  <span class="compare-price">{{ product.compare_at_price | money }}</span>
                {% endif %}
              </div>
              <button class="add-to-cart-btn" onclick="location.href='{{ product.url }}'">Add to Cart</button>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </section>

  <!-- Newsletter Section -->
  <section class="newsletter-section">
    <div class="page-width">
      <h2 class="newsletter-title">Stay Updated</h2>
      <p class="newsletter-description">Get the latest deals and product updates delivered to your inbox</p>
      <form
        action="{{ routes.root_url }}contact#contact_form"
        method="post"
        id="contact_form"
        accept-charset="UTF-8"
        class="newsletter-form"
      >
        <input type="hidden" name="form_type" value="customer">
        <input type="hidden" name="utf8" value="✓">
        <input
          type="email"
          name="contact[email]"
          class="newsletter-input"
          placeholder="Enter your email"
          required
        >
        <button type="submit" class="newsletter-button">Subscribe</button>
      </form>
    </div>
  </section>
</div>
