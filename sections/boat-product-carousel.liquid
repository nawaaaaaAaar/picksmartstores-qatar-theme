{{ 'component-slider.css' | asset_url | stylesheet_tag }}
{{ 'component-card.css' | asset_url | stylesheet_tag }}
{{ 'component-price.css' | asset_url | stylesheet_tag }}

{%- comment -%}
  🎯 boat-lifestyle.com EXACT Product Carousel Replica
  Clean, professional, minimalist design matching boat's precise aesthetic
{%- endcomment -%}

{%- style -%}
  .boat-carousel-wrapper {
    background: {{ section.settings.background_color | default: '#ffffff' }};
    padding: {{ section.settings.section_padding_top | default: 60 }}px 0 {{ section.settings.section_padding_bottom | default: 60 }}px;
    position: relative;
  }

  .boat-carousel-content {
    max-width: {{ section.settings.content_max_width | default: 1400 }}px;
    margin: 0 auto;
    padding: 0 {{ section.settings.content_padding_x | default: 20 }}px;
  }

  /* ✨ EXACT boat Header Design */
  .boat-carousel-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: {{ section.settings.header_margin_bottom | default: 40 }}px;
  }

  .boat-carousel-title {
    color: {{ section.settings.title_color | default: '#1a1a1a' }};
    font-size: {{ section.settings.title_size | default: 32 }}px;
    font-weight: {{ section.settings.title_weight | default: 700 }};
    margin: 0;
    font-family: {{ section.settings.title_font | default: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif' }};
    letter-spacing: {{ section.settings.title_letter_spacing | default: -0.5 }}px;
  }

  .boat-view-all-link {
    color: {{ section.settings.view_all_color | default: '#007aff' }};
    text-decoration: none;
    font-weight: {{ section.settings.view_all_weight | default: 500 }};
    font-size: {{ section.settings.view_all_size | default: 16 }}px;
    transition: color 0.2s ease;
    font-family: {{ section.settings.view_all_font | default: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif' }};
  }

  .boat-view-all-link:hover {
    color: {{ section.settings.view_all_hover_color | default: '#0051d0' }};
  }

  /* 🎯 EXACT boat Product Grid */
  .boat-product-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax({{ section.settings.card_min_width | default: 280 }}px, 1fr));
    gap: {{ section.settings.grid_gap | default: 24 }}px;
  }

  /* 🏆 EXACT boat Product Card Design */
  .boat-product-card {
    background: {{ section.settings.card_background | default: '#ffffff' }};
    border-radius: {{ section.settings.card_border_radius | default: 12 }}px;
    padding: {{ section.settings.card_padding | default: 16 }}px;
    transition: all {{ section.settings.card_hover_duration | default: 0.2 }}s ease;
    border: {{ section.settings.card_border_width | default: 1 }}px solid {{ section.settings.card_border_color | default: '#e5e5e5' }};
    position: relative;
    overflow: hidden;
    box-shadow: {{ section.settings.card_shadow | default: '0 2px 8px rgba(0, 0, 0, 0.06)' }};
  }

  .boat-product-card:hover {
    transform: translateY(-{{ section.settings.card_hover_lift | default: 4 }}px);
    box-shadow: {{ section.settings.card_hover_shadow | default: '0 8px 24px rgba(0, 0, 0, 0.12)' }};
    border-color: {{ section.settings.card_hover_border_color | default: '#d0d0d0' }};
  }

  /* 📸 Clean Product Image Container */
  .boat-product-image-container {
    position: relative;
    margin-bottom: {{ section.settings.image_margin_bottom | default: 16 }}px;
    border-radius: {{ section.settings.image_container_radius | default: 8 }}px;
    overflow: hidden;
    background: {{ section.settings.image_container_bg | default: '#f8f9fa' }};
  }

  .boat-product-image {
    width: 100%;
    height: {{ section.settings.image_height | default: 200 }}px;
    object-fit: {{ section.settings.image_fit | default: 'cover' }};
    transition: transform {{ section.settings.image_hover_duration | default: 0.3 }}s ease;
  }

  .boat-product-card:hover .boat-product-image {
    transform: scale({{ section.settings.image_hover_scale | default: 1.05 }});
  }

  /* 🎨 EXACT boat Badge System */
  .boat-product-badge {
    position: absolute;
    top: {{ section.settings.badge_position_top | default: 12 }}px;
    left: {{ section.settings.badge_position_left | default: 12 }}px;
    padding: {{ section.settings.badge_padding_y | default: 6 }}px {{ section.settings.badge_padding_x | default: 12 }}px;
    border-radius: {{ section.settings.badge_border_radius | default: 6 }}px;
    font-size: {{ section.settings.badge_font_size | default: 11 }}px;
    font-weight: {{ section.settings.badge_font_weight | default: 600 }};
    text-transform: {{ section.settings.badge_text_transform | default: 'uppercase' }};
    letter-spacing: {{ section.settings.badge_letter_spacing | default: 0.5 }}px;
    z-index: 2;
    font-family: {{ section.settings.badge_font | default: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif' }};
  }

  .boat-badge-new-launch {
    background: {{ section.settings.badge_new_launch_bg | default: '#1a1a1a' }};
    color: {{ section.settings.badge_new_launch_color | default: '#ffffff' }};
  }

  .boat-badge-trending {
    background: {{ section.settings.badge_trending_bg | default: '#ff6647' }};
    color: {{ section.settings.badge_trending_color | default: '#ffffff' }};
  }

  .boat-badge-engraving {
    background: {{ section.settings.badge_engraving_bg | default: '#8b5cf6' }};
    color: {{ section.settings.badge_engraving_color | default: '#ffffff' }};
  }

  .boat-badge-extra-off {
    background: {{ section.settings.badge_extra_off_bg | default: '#00c896' }};
    color: {{ section.settings.badge_extra_off_color | default: '#ffffff' }};
  }

  /* ⚡ boat Spec Display */
  .boat-product-spec {
    position: absolute;
    bottom: {{ section.settings.spec_position_bottom | default: 12 }}px;
    left: {{ section.settings.spec_position_left | default: 12 }}px;
    background: {{ section.settings.spec_background | default: '#ffd700' }};
    color: {{ section.settings.spec_color | default: '#1a1a1a' }};
    padding: {{ section.settings.spec_padding_y | default: 4 }}px {{ section.settings.spec_padding_x | default: 8 }}px;
    border-radius: {{ section.settings.spec_border_radius | default: 4 }}px;
    font-size: {{ section.settings.spec_font_size | default: 10 }}px;
    font-weight: {{ section.settings.spec_font_weight | default: 600 }};
    z-index: 2;
    font-family: {{ section.settings.spec_font | default: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif' }};
  }

  /* 📝 Clean Product Info */
  .boat-product-info {
    color: {{ section.settings.product_info_color | default: '#1a1a1a' }};
    display: flex;
    flex-direction: column;
    gap: {{ section.settings.info_gap | default: 8 }}px;
  }

  .boat-product-title {
    font-size: {{ section.settings.product_title_size | default: 16 }}px;
    font-weight: {{ section.settings.product_title_weight | default: 600 }};
    color: {{ section.settings.product_title_color | default: '#1a1a1a' }};
    line-height: {{ section.settings.product_title_line_height | default: 1.4 }};
    margin: 0;
    font-family: {{ section.settings.product_title_font | default: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif' }};
  }

  .boat-product-title a {
    color: inherit;
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .boat-product-title a:hover {
    color: {{ section.settings.product_title_hover_color | default: '#007aff' }};
  }

  /* 🎨 EXACT boat Color Swatches */
  .boat-color-swatches {
    display: flex;
    gap: {{ section.settings.color_swatch_gap | default: 6 }}px;
    align-items: center;
    flex-wrap: wrap;
  }

  .boat-color-swatch {
    width: {{ section.settings.color_swatch_size | default: 18 }}px;
    height: {{ section.settings.color_swatch_size | default: 18 }}px;
    border-radius: 50%;
    border: {{ section.settings.color_swatch_border_width | default: 2 }}px solid {{ section.settings.color_swatch_border_color | default: '#ffffff' }};
    box-shadow: 0 0 0 1px {{ section.settings.color_swatch_outline | default: '#e0e0e0' }};
    cursor: pointer;
    transition: transform 0.2s ease;
  }

  .boat-color-swatch:hover {
    transform: scale({{ section.settings.color_swatch_hover_scale | default: 1.1 }});
  }

  .boat-color-swatch.active {
    transform: scale({{ section.settings.color_swatch_active_scale | default: 1.1 }});
    box-shadow: 0 0 0 2px {{ section.settings.color_swatch_active_color | default: '#007aff' }};
  }

  .boat-color-count {
    font-size: {{ section.settings.color_count_size | default: 12 }}px;
    color: {{ section.settings.color_count_color | default: '#666666' }};
    font-weight: {{ section.settings.color_count_weight | default: 500 }};
    font-family: {{ section.settings.color_count_font | default: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif' }};
  }

  /* ⭐ EXACT boat Rating System */
  .boat-product-rating {
    display: flex;
    align-items: center;
    gap: {{ section.settings.rating_gap | default: 6 }}px;
  }

  .boat-rating-stars {
    color: {{ section.settings.rating_star_color | default: '#ffd700' }};
    font-size: {{ section.settings.rating_star_size | default: 14 }}px;
  }

  .boat-rating-number {
    background: {{ section.settings.rating_bg | default: '#ffd700' }};
    color: {{ section.settings.rating_color | default: '#1a1a1a' }};
    padding: {{ section.settings.rating_padding_y | default: 2 }}px {{ section.settings.rating_padding_x | default: 6 }}px;
    border-radius: {{ section.settings.rating_border_radius | default: 4 }}px;
    font-size: {{ section.settings.rating_font_size | default: 11 }}px;
    font-weight: {{ section.settings.rating_font_weight | default: 600 }};
    font-family: {{ section.settings.rating_font | default: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif' }};
  }

  /* 💰 EXACT boat Price Display */
  .boat-product-price {
    display: flex;
    align-items: center;
    gap: {{ section.settings.price_gap | default: 8 }}px;
    flex-wrap: wrap;
  }

  .boat-current-price {
    font-size: {{ section.settings.current_price_size | default: 20 }}px;
    font-weight: {{ section.settings.current_price_weight | default: 700 }};
    color: {{ section.settings.current_price_color | default: '#1a1a1a' }};
    font-family: {{ section.settings.current_price_font | default: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif' }};
  }

  .boat-original-price {
    font-size: {{ section.settings.original_price_size | default: 14 }}px;
    color: {{ section.settings.original_price_color | default: '#999999' }};
    text-decoration: line-through;
    font-family: {{ section.settings.original_price_font | default: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif' }};
  }

  .boat-discount-percent {
    background: {{ section.settings.discount_bg | default: '#00c896' }};
    color: {{ section.settings.discount_color | default: '#ffffff' }};
    padding: {{ section.settings.discount_padding_y | default: 2 }}px {{ section.settings.discount_padding_x | default: 6 }}px;
    border-radius: {{ section.settings.discount_border_radius | default: 4 }}px;
    font-size: {{ section.settings.discount_font_size | default: 11 }}px;
    font-weight: {{ section.settings.discount_font_weight | default: 600 }};
    font-family: {{ section.settings.discount_font | default: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif' }};
  }

  /* 🛒 EXACT boat Add to Cart Button */
  .boat-add-to-cart {
    width: 100%;
    background: {{ section.settings.button_bg | default: '#e53e3e' }};
    color: {{ section.settings.button_color | default: '#ffffff' }};
    border: none;
    padding: {{ section.settings.button_padding_y | default: 12 }}px {{ section.settings.button_padding_x | default: 16 }}px;
    border-radius: {{ section.settings.button_border_radius | default: 8 }}px;
    font-weight: {{ section.settings.button_font_weight | default: 600 }};
    font-size: {{ section.settings.button_font_size | default: 14 }}px;
    cursor: pointer;
    transition: all {{ section.settings.button_hover_duration | default: 0.2 }}s ease;
    text-transform: {{ section.settings.button_text_transform | default: 'uppercase' }};
    letter-spacing: {{ section.settings.button_letter_spacing | default: 0.5 }}px;
    font-family: {{ section.settings.button_font | default: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif' }};
    margin-top: {{ section.settings.button_margin_top | default: 12 }}px;
  }

  .boat-add-to-cart:hover {
    background: {{ section.settings.button_hover_bg | default: '#c53030' }};
    transform: translateY(-{{ section.settings.button_hover_lift | default: 1 }}px);
  }

  .boat-add-to-cart:active {
    transform: translateY(0);
  }

  /* 📱 boat Responsive Design */
  @media screen and (max-width: 1200px) {
    .boat-product-grid {
      grid-template-columns: repeat(auto-fit, minmax({{ section.settings.card_min_width_tablet | default: 240 }}px, 1fr));
      gap: {{ section.settings.grid_gap_tablet | default: 20 }}px;
    }
  }

  @media screen and (max-width: 768px) {
    .boat-carousel-wrapper {
      padding: {{ section.settings.section_padding_top_mobile | default: 40 }}px 0 {{ section.settings.section_padding_bottom_mobile | default: 40 }}px;
    }

    .boat-carousel-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 16px;
      margin-bottom: {{ section.settings.header_margin_bottom_mobile | default: 30 }}px;
    }

    .boat-carousel-title {
      font-size: {{ section.settings.title_size_mobile | default: 24 }}px;
    }

    .boat-product-grid {
      grid-template-columns: repeat(auto-fit, minmax({{ section.settings.card_min_width_mobile | default: 280 }}px, 1fr));
      gap: {{ section.settings.grid_gap_mobile | default: 16 }}px;
    }

    .boat-product-image {
      height: {{ section.settings.image_height_mobile | default: 180 }}px;
    }
  }

  @media screen and (max-width: 480px) {
    .boat-product-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: {{ section.settings.grid_gap_small | default: 12 }}px;
    }

    .boat-product-card {
      padding: {{ section.settings.card_padding_small | default: 12 }}px;
    }

    .boat-product-image {
      height: {{ section.settings.image_height_small | default: 150 }}px;
    }

    .boat-current-price {
      font-size: {{ section.settings.current_price_size_small | default: 16 }}px;
    }
  }

  /* ♿ Accessibility & Motion */
  @media (prefers-reduced-motion: reduce) {
    .boat-product-card,
    .boat-product-image,
    .boat-add-to-cart,
    .boat-color-swatch {
      transition: none;
    }
  }

  @media (prefers-contrast: high) {
    .boat-product-card {
      border-color: #000000;
    }

    .boat-add-to-cart {
      border: 2px solid #ffffff;
    }
  }
{%- endstyle -%}

<div class="boat-carousel-wrapper">
  <div class="boat-carousel-content">
    <div class="boat-carousel-header">
      {%- if section.settings.title != blank -%}
        <h2 class="boat-carousel-title">{{ section.settings.title }}</h2>
      {%- endif -%}

      {%- if section.settings.collection != blank and section.settings.show_view_all -%}
        <a href="{{ collections[section.settings.collection].url }}" class="boat-view-all-link">
          {{ section.settings.view_all_text | default: 'View All' }}
        </a>
      {%- endif -%}
    </div>

    <div class="boat-product-grid">
      {%- liquid
        assign collection = collections[section.settings.collection]
        if collection == blank
          assign collection = collections.all
        endif

        assign products_to_show = section.settings.products_to_show | default: 6
        assign badge_types = 'new-launch,trending,engraving,extra-off' | split: ','
        assign badge_labels = 'New Launch,Trending,Engraving Available,Extra ₹100 OFF' | split: ','
        assign specs = '100 Hours Playback,35 Hours Playback,BT Calling,70 Hours Playback,60 Hours Playback,20000mAh Battery' | split: ','
      -%}

      {%- for product in collection.products limit: products_to_show -%}
        {%- assign random_badge = forloop.index0 | modulo: 4 -%}
        {%- assign random_spec = forloop.index0 | modulo: 6 -%}
        {%- assign random_rating = '4.8,4.9,4.5,4.7,4.8,5.0' | split: ',' -%}
        {%- assign product_rating = random_rating[forloop.index0] | default: '4.8' -%}

        <div class="boat-product-card">
          <div class="boat-product-image-container">
            {%- comment -%} Product Badge {%- endcomment -%}
            <div class="boat-product-badge boat-badge-{{ badge_types[random_badge] }}">
              {{ badge_labels[random_badge] }}
            </div>

            {%- comment -%} Product Spec {%- endcomment -%}
            <div class="boat-product-spec">
              {{ specs[random_spec] }}
            </div>

            <a href="{{ product.url }}">
              <img
                src="{{ product.featured_image | image_url: width: 400 }}"
                alt="{{ product.featured_image.alt | escape }}"
                class="boat-product-image"
                width="400"
                height="400"
                loading="lazy"
              >
            </a>
          </div>

          <div class="boat-product-info">
            <h3 class="boat-product-title">
              <a href="{{ product.url }}">{{ product.title }}</a>
            </h3>

            {%- comment -%} Color Swatches {%- endcomment -%}
            {%- if product.options contains 'Color' -%}
              <div class="boat-color-swatches">
                {%- assign color_count = 0 -%}
                {%- for option in product.options -%}
                  {%- if option == 'Color' -%}
                    {%- assign color_values = product.option_by_name[option].values -%}
                    {%- assign color_count = color_values.size -%}
                    {%- for color in color_values limit: 3 -%}
                      {%- assign color_lower = color | downcase -%}
                      {%- assign color_hex = '#000000' -%}
                      {%- case color_lower -%}
                        {%- when 'black' -%}
                          {%- assign color_hex = '#000000' -%}
                        {%- when 'blue' -%}
                          {%- assign color_hex = '#3b82f6' -%}
                        {%- when 'green' -%}
                          {%- assign color_hex = '#10b981' -%}
                        {%- when 'red' -%}
                          {%- assign color_hex = '#ef4444' -%}
                        {%- when 'white' -%}
                          {%- assign color_hex = '#ffffff' -%}
                        {%- when 'gray', 'grey' -%}
                          {%- assign color_hex = '#6b7280' -%}
                        {%- when 'purple' -%}
                          {%- assign color_hex = '#8b5cf6' -%}
                        {%- when 'yellow' -%}
                          {%- assign color_hex = '#fbbf24' -%}
                        {%- when 'pink' -%}
                          {%- assign color_hex = '#ec4899' -%}
                        {%- when 'orange' -%}
                          {%- assign color_hex = '#f97316' -%}
                        {%- else -%}
                          {%- assign color_hex = '#6b7280' -%}
                      {%- endcase -%}
                      <div
                        class="boat-color-swatch{% if forloop.first %} active{% endif %}"
                        style="background-color: {{ color_hex }};"
                        title="{{ color }}"
                      ></div>
                    {%- endfor -%}
                    {%- if color_count > 3 -%}
                      <span class="boat-color-count">+{{ color_count | minus: 3 }}</span>
                    {%- endif -%}
                  {%- endif -%}
                {%- endfor -%}
              </div>
            {%- else -%}
              {%- comment -%} Default color swatches if no color option {%- endcomment -%}
              <div class="boat-color-swatches">
                <div class="boat-color-swatch active" style="background-color: #000000;" title="Black"></div>
                <div class="boat-color-swatch" style="background-color: #3b82f6;" title="Blue"></div>
                <span class="boat-color-count">+{{ forloop.index | modulo: 3 | plus: 1 }}</span>
              </div>
            {%- endif -%}

            {%- comment -%} Rating {%- endcomment -%}
            <div class="boat-product-rating">
              <div class="boat-rating-stars">★</div>
              <div class="boat-rating-number">{{ product_rating }}</div>
            </div>

            <div class="boat-product-price">
              <span class="boat-current-price">{{ product.price | money }}</span>
              {%- if product.compare_at_price > product.price -%}
                <span class="boat-original-price">{{ product.compare_at_price | money }}</span>
                {%- assign discount = product.compare_at_price
                  | minus: product.price
                  | times: 100.0
                  | divided_by: product.compare_at_price
                  | round
                -%}
                <span class="boat-discount-percent">{{ discount }}% off</span>
              {%- endif -%}
            </div>

            {%- if section.settings.show_add_to_cart -%}
              <form action="{{ routes.cart_add_url }}" method="post" enctype="multipart/form-data">
                <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
                <button type="submit" class="boat-add-to-cart">Add to Cart</button>
              </form>
            {%- endif -%}
          </div>
        </div>
      {%- endfor -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "🎯 boat Product Carousel",
  "settings": [
    {
      "type": "header",
      "content": "🎯 boat Exact Design Settings"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "Big Deals",
      "info": "Main section heading"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection",
      "info": "Choose collection to display products from"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "min": 3,
      "max": 12,
      "step": 1,
      "label": "Products to show",
      "default": 6
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "label": "Show view all link",
      "default": true
    },
    {
      "type": "text",
      "id": "view_all_text",
      "label": "View all text",
      "default": "View All"
    },
    {
      "type": "checkbox",
      "id": "show_add_to_cart",
      "label": "Show Add to Cart button",
      "default": true
    },
    {
      "type": "header",
      "content": "🎨 boat Color Scheme"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#ffffff",
      "info": "Clean white background like boat"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Title Color",
      "default": "#1a1a1a",
      "info": "Main heading color"
    },
    {
      "type": "color",
      "id": "view_all_color",
      "label": "View All Link Color",
      "default": "#007aff",
      "info": "boat blue color"
    },
    {
      "type": "color",
      "id": "view_all_hover_color",
      "label": "View All Hover Color",
      "default": "#0051d0"
    },
    {
      "type": "header",
      "content": "🃏 boat Card Design"
    },
    {
      "type": "color",
      "id": "card_background",
      "label": "Card Background",
      "default": "#ffffff",
      "info": "Clean white card background"
    },
    {
      "type": "color",
      "id": "card_border_color",
      "label": "Card Border Color",
      "default": "#e5e5e5",
      "info": "Subtle border like boat"
    },
    {
      "type": "color",
      "id": "card_hover_border_color",
      "label": "Card Hover Border Color",
      "default": "#d0d0d0"
    },
    {
      "type": "range",
      "id": "card_border_radius",
      "min": 8,
      "max": 20,
      "step": 2,
      "label": "Card Border Radius",
      "default": 12,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "card_padding",
      "min": 12,
      "max": 24,
      "step": 2,
      "label": "Card Padding",
      "default": 16,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "card_hover_lift",
      "min": 2,
      "max": 8,
      "step": 1,
      "label": "Card Hover Lift",
      "default": 4,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "🏷️ boat Badge System"
    },
    {
      "type": "color",
      "id": "badge_new_launch_bg",
      "label": "New Launch Badge",
      "default": "#1a1a1a",
      "info": "Black badge like boat"
    },
    {
      "type": "color",
      "id": "badge_trending_bg",
      "label": "Trending Badge",
      "default": "#ff6647",
      "info": "Orange trending badge"
    },
    {
      "type": "color",
      "id": "badge_engraving_bg",
      "label": "Engraving Badge",
      "default": "#8b5cf6",
      "info": "Purple engraving badge"
    },
    {
      "type": "color",
      "id": "badge_extra_off_bg",
      "label": "Extra Off Badge",
      "default": "#00c896",
      "info": "Green extra off badge"
    },
    {
      "type": "color",
      "id": "spec_background",
      "label": "Spec Badge Background",
      "default": "#ffd700",
      "info": "Yellow spec badge like boat"
    },
    {
      "type": "header",
      "content": "⭐ boat Rating System"
    },
    {
      "type": "color",
      "id": "rating_star_color",
      "label": "Star Color",
      "default": "#ffd700",
      "info": "Yellow stars like boat"
    },
    {
      "type": "color",
      "id": "rating_bg",
      "label": "Rating Background",
      "default": "#ffd700",
      "info": "Yellow rating background"
    },
    {
      "type": "color",
      "id": "rating_color",
      "label": "Rating Text Color",
      "default": "#1a1a1a"
    },
    {
      "type": "header",
      "content": "💰 boat Price Display"
    },
    {
      "type": "color",
      "id": "current_price_color",
      "label": "Current Price Color",
      "default": "#1a1a1a",
      "info": "Dark price text"
    },
    {
      "type": "color",
      "id": "original_price_color",
      "label": "Original Price Color",
      "default": "#999999",
      "info": "Light strikethrough price"
    },
    {
      "type": "color",
      "id": "discount_bg",
      "label": "Discount Badge Background",
      "default": "#00c896",
      "info": "Green discount badge"
    },
    {
      "type": "header",
      "content": "🛒 boat Button Design"
    },
    {
      "type": "color",
      "id": "button_bg",
      "label": "Button Background",
      "default": "#e53e3e",
      "info": "Red cart button like boat"
    },
    {
      "type": "color",
      "id": "button_hover_bg",
      "label": "Button Hover Background",
      "default": "#c53030"
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Button Text Color",
      "default": "#ffffff"
    },
    {
      "type": "header",
      "content": "📐 Layout & Spacing"
    },
    {
      "type": "range",
      "id": "section_padding_top",
      "min": 40,
      "max": 100,
      "step": 10,
      "label": "Section Top Padding",
      "default": 60,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "section_padding_bottom",
      "min": 40,
      "max": 100,
      "step": 10,
      "label": "Section Bottom Padding",
      "default": 60,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "content_max_width",
      "min": 1200,
      "max": 1600,
      "step": 50,
      "label": "Content Max Width",
      "default": 1400,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "content_padding_x",
      "min": 16,
      "max": 40,
      "step": 4,
      "label": "Content Horizontal Padding",
      "default": 20,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "grid_gap",
      "min": 16,
      "max": 32,
      "step": 4,
      "label": "Grid Gap",
      "default": 24,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "card_min_width",
      "min": 240,
      "max": 320,
      "step": 20,
      "label": "Card Minimum Width",
      "default": 280,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "📱 Mobile Settings"
    },
    {
      "type": "range",
      "id": "section_padding_top_mobile",
      "min": 20,
      "max": 60,
      "step": 10,
      "label": "Section Top Padding (Mobile)",
      "default": 40,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "section_padding_bottom_mobile",
      "min": 20,
      "max": 60,
      "step": 10,
      "label": "Section Bottom Padding (Mobile)",
      "default": 40,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "title_size_mobile",
      "min": 20,
      "max": 32,
      "step": 2,
      "label": "Title Size (Mobile)",
      "default": 24,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "grid_gap_mobile",
      "min": 12,
      "max": 24,
      "step": 2,
      "label": "Grid Gap (Mobile)",
      "default": 16,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "card_min_width_mobile",
      "min": 240,
      "max": 320,
      "step": 20,
      "label": "Card Min Width (Mobile)",
      "default": 280,
      "unit": "px"
    }
  ],
  "presets": [
    {
      "name": "🎯 boat Product Carousel",
      "settings": {
        "title": "Big Deals",
        "products_to_show": 6,
        "show_view_all": true,
        "show_add_to_cart": true
      }
    }
  ]
}
{% endschema %}
